# Backend API

## How to run backend locally

1. Open terminal in backend folder
2. Run:

npm install

3. Start the server:

npm run dev

4. Open:

http://localhost:5000

The backend will start on port 5000.